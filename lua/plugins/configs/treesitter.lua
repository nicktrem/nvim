local options = {
  ensure_installed = { "lua", "c", "cpp", "python", "typescript", "javascript", "rust", "solidity", "bash", "json", "yaml", "toml" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = true },
}

return options
